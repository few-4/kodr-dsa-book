// Q11	Find circumference and area of circle

C = 2 PI r
Area = PI r(square)

let r = Number(prompt("Give me radius of circle: "));

let circumference = 2 * (Math.PI * r);
let area = Math.PI * r**2

console.log(area)